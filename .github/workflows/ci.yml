name: CI-CD-bankapp

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  build-test-scan-deploy:
    runs-on: ubuntu-latest

    steps:

    - name: Checkout Code
      uses: actions/checkout@v4

    - name: Set up Java 21
      uses: actions/setup-java@v4
      with:
        distribution: temurin
        java-version: '21'

    - name: Cache Maven Dependencies
      uses: actions/cache@v4
      with:
        path: ~/.m2
        key: maven-${{ hashFiles('**/pom.xml') }}

    - name: Build and Test
      run: ./mvnw clean verify

    - name: Run Trivy FS Scan
      uses: aquasecurity/trivy-action@0.35.0
      with:
        scan-type: fs
        format: table
        exit-code: '1'
        severity: CRITICAL,HIGH

    - name: Build Docker Image
      run: docker build -t ${{ vars.DOCKER_USERNAME }}/bankapp:${{ github.sha }} .

    - name: Run Trivy Image Scan
      uses: aquasecurity/trivy-action@0.35.0
      with:
        image-ref: ${{ vars.DOCKER_USERNAME }}/bankapp:${{ github.sha }}
        format: table
        exit-code: '1'
        severity: CRITICAL,HIGH

    - name: Docker Hub Login
      uses: docker/login-action@v3
      with:
        username: ${{ vars.DOCKER_USERNAME }}
        password: ${{ secrets.DOCKER_PASSWORD }}

    - name: Push Docker Image
      run: docker push ${{ vars.DOCKER_USERNAME }}/bankapp:${{ github.sha }}

  #  - name: Set kubeconfig               (Terraform integration in progress)
   #   run: |
    #    mkdir -p $HOME/.kube
     #   echo "${{ secrets.KUBECONFIG }}" > $HOME/.kube/config

  #  - name: Deploy to Kubernetes
   #   run: |
    #    kubectl set image deployment/bankapp-deployment bankapp=${{ vars.DOCKER_USERNAME }}/bankapp:${{ github.sha }} -n bank-app

  #  - name: Verify Deployment
   #   run: |
    #    kubectl rollout status deployment/bankapp-deployment -n bank-app

  #  - name: Rollback on Failure
   #   if: failure()
    #  run: |
     #   kubectl rollout undo deployment/bankapp-deployment -n bank-app